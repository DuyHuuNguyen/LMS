spring:
  config:
    activate:
      on-profile: dev
    import: optional:file:.env[.properties],optional:file:../.env[.properties]
  datasource:
    url: ${USER_DB_URL_DEV:jdbc:postgresql://localhost:5432/db_user_service_lms}
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  cache:
    type: redis
    redis:
      time-to-live: 600000ms
  data:
    redis:
      host: ${REDIS_HOST_DEV:localhost}
      port: ${REDIS_PORT_DEV:6379}
  rabbitmq:
    port: 5672
    host: ${RABBIT_MQ_DEV:localhost}
    username: guest
    password: guest
  mail:
    host: smtp.gmail.com
    port: 587
    username: hotrotoyworld@gmail.com
    password: tknm zdhx fiox vazw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  threads:
    virtual:
      enabled: true


jwt:
  secretKey: ${JWT_SECRET_KEY}
  accessTokenExpirationTime: 259200000 # 3 days
  refreshTokenExpirationTime: 2419200000 # 2 weeks
  resetPasswordTokenExpirationTime: 900000 # 15 minutes

rabbitmq:
  exchange-name: demo
  user-mail-routing-key: demo
  user-mail-queue: lms-queue

  cache-data-queue: cache-data-queue

server:
  port: 8083

cloudinary:
  cloudinary-name: ${CLOUDINARY_NAME_OF_USER_SERVICE}
  api-key: ${CLOUDINARY_API_KEY_OF_USER_SERVICE}
  api-secret: ${CLOUDINARY_API_SECRET_OF_USER_SERVICE}

grpc:
  server:
    security:
      enabled: ${GRPC_SERVER_SECURITY_ENABLED:false}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

curriculum-service: CURRICULUM-SERVICE