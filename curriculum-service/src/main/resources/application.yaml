spring:
  application:
    name: LMS
  datasource:
    url: jdbc:postgresql://localhost:5432/db_curriculum_service_lms
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8085
---
grpc:
  client:
    parse-token:
      address: 'static://localhost:9090'
      negotiationType: plaintext